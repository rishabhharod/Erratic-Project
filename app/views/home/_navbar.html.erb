<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <span class="ms-4 fs-3">Erratic</span>
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
      <div class="d-flex align-items-center">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-center">
          <% if user_signed_in? %>
            <li class="nav-item mx-2 ">
              <% if current_user.profile.attached? %>
                <%= image_tag current_user.profile, style:"height:50px;width:50px;border-radius:50%" %>
              <% end %>
            </li>
            <% if current_user.role.eql?('seller') %>
              <% if current_user.seller.present? %>
                <li class="nav-item mx-5">
                  <%= link_to 'Product Details',sellerProduct_path %>
                </li>
              <% else %>
                <li class="nav-item mx-5">
                  <%= link_to 'Enter Seller Details',new_sellers_path %>
                </li>
              <% end %>
              <li class="nav-item mx-5">
                <%= link_to 'Add Product',new_product_path %>
              </li>
            <% end %>
            <li class="nav-item mx-5">
              <%= link_to root_path do %>
                <i class="fas fa-home fa-lg"></i>
              <% end %>
            </li>
            <li class="nav-item mx-5 ">
              <%= link_to image_tag('cart1.svg',alt: 'cart logo',title:'Cart', style:'height:30px; width:30px'), carts_path(current_user) %>
            </li>
            <li class="nav-item mx-5">
              <%= link_to image_tag('heart.svg',alt: 'wishlist logo',title:'Wishlist', style:'height:28 px; width:28px'), wishlist_path(current_user) %>
            </li>
            <li class="nav-item mx-5">
              <%= link_to image_tag('person.svg',alt: 'profile logo',title:'Profile', style:'height:30px; width:30px'), edit_user_registration_path	 %>
            </li>
            <li class="nav-item mx-5">
              <%= button_to destroy_user_session_path, method: :delete, class: 'logout-link',title:'logout' do %>
              <i class="fas fa-sign-out-alt fa-lg"></i>
            <% end %>
            </li>
          <% else %>
            <li class="nav-item mx-5">
              <%= button_to 'Sign in', new_user_session_path,class:"btn btn-outline-info" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
